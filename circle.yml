general:
  build_dir: dist
deployment:
  production:
    branch: master
    commands:
      - chmod +x deploy-production.sh
      - ./deploy-production.sh
  staging:
    branch: staging
    commands:
      - git push -f https://github.com/manjeshpv/circleci-test.dist
