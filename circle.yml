deployment:
  production:
    branch: master
    commands:
      - chmod +x deploy-production.sh
      - ./deploy-production.sh
  production:
    branch: staging
    commands:
      - chmod +x deploy-staging.sh
      - ./deploy-staging.sh